import styled from 'styled-components';
import { Colors } from '../Global';

export const AddNewCityButton = styled.button`
  background: ${Colors.primary};
  color: ${Colors.white};
  border: none;
  padding: 8px;
  cursor: pointer;
  border-radius: 4px;
`;

export const FormWrapper = styled.div``;
